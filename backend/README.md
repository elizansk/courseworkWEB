
Вот полный технический отчёт в Markdown с учётом твоих данных и рекомендаций — прям готовый к использованию для документации или передачи фронтендеру:

```markdown
# 📋 Полный технический отчёт: API для LMS на Django

## 🧩 Архитектура системы

### Стек технологий

- **Backend**: Django 4.2 + Django REST Framework + JWT
- **Database**: PostgreSQL 15 (в Docker)
- **Frontend**: Любой фронтенд-фреймворк (React, Vue, Vanilla JS)
- **Аутентификация**: JWT-токены (RFC 7519)
- **Безопасность**: Хеширование паролей (PBKDF2), CORS, транзакции

### Диаграмма взаимодействия

```

2. **Django обрабатывает запрос** :

* Проверяет совпадение паролей
* Создаёт запись в таблице** **`users`
* **Хеширует пароль** с помощью PBKDF2 → сохраняет хеш
* Автоматически добавляет роль студента (3) в таблицу** **`users_roles`

3. **SQL-запросы** :

```sql
INSERT INTO users (email, first_name, last_name, phone, password_hash, ...)
VALUES ('user@example.com', 'Иван', 'Иванов', '+79991234567', 'pbkdf2_sha256$...', ...);

INSERT INTO users_roles (user_id, role_id) VALUES (9, 3);
```

### Процесс входа

1. **Фронтенд отправляет** :

```json
POST /api/v1/auth/login/
{
  "email": "user@example.com",
  "password": "strongpassword123"
}
```

2. **Django проверяет учётные данные** :

* Находит пользователя по email
* Сравнивает хеш присланного пароля с** **`password_hash` из БД
* Генерирует JWT-токены

3. **Пример ответа** :

```json
{
  "access": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refresh": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user_id": 9,
  "email": "user@example.com",
  "roles": [3]
}
```

---

## 🗂️ Эндпоинты API

### 1. Публичные (доступны всем)

| Эндпоинт               | Метод | Описание                                                           |
| ------------------------------ | ---------- | -------------------------------------------------------------------------- |
| `/courses/`                  | GET        | Список курсов (последние 10)                          |
| `/courses/{slug}/`           | GET        | Детали курса со всеми модулями и уроками |
| `/instructors/{id}/`         | GET        | Профиль преподавателя                                  |
| `/instructors/{id}/courses/` | GET        | Курсы преподавателя                                      |

### 2. Защищённые (требуют JWT)

| Эндпоинт              | Метод | Описание                                                             |
| ----------------------------- | ---------- | ---------------------------------------------------------------------------- |
| `/profile/`                 | GET        | Профиль текущего пользователя                     |
| `/profile/enrollments/`     | GET        | Записи пользователя на курсы (только active)  |
| `/courses/{slug}/learning/` | GET        | Страница обучения (только для записанных) |
| `/lessons/{id}/`            | GET        | Детали урока (только если есть доступ)        |
| `/enrollments/`             | POST       | Запись на курс + платёж (в транзакции)          |
| `/ratings/`                 | POST       | Оставить оценку (только 1 раз на курс)          |

### 3. Эндпоинты администратора

| Префикс      | Доступ | Описание                                  |
| ------------------- | ------------ | ------------------------------------------------- |
| `/admin/users/`   | role_id=1    | Управление пользователями |
| `/admin/courses/` | role_id=1    | Управление курсами               |
| `/admin/modules/` | role_id=1    | Управление модулями             |

---

## 🛠️ Работа с данными

### Пример: запись на курс + платёж

```python
@transaction.atomic
def perform_create(self, serializer):
    enrollment = serializer.save(
        user=self.request.user,
        status='active',
        progress_pct=0
    )
    Payment.objects.create(
        user=self.request.user,
        course=enrollment.course,
        amount=enrollment.course.price,
        status='completed'
    )
```

SQL:

```sql
BEGIN;

INSERT INTO enrollments (user_id, course_id, status, progress_pct, enrolled_at)
VALUES (5, 1, 'active', 0, '2025-12-19 12:00:00');

INSERT INTO payments (user_id, course_id, amount, status, paid_at)
VALUES (5, 1, 2990.00, 'completed', '2025-12-19 12:00:00');

COMMIT;
```

### Оптимизация чтения (prefetch_related)

```python
def get_modules(self, obj):
    modules = obj.module_set.filter(is_deleted=False).order_by('order_num')
    return ModuleSerializer(modules, many=True, context=self.context).data
```

* Один запрос для курса
* Один запрос для всех модулей курса
* Один запрос для всех уроков модулей
* Без оптимизации было бы N+1 запросов

---

## 🔒 Безопасность и валидация

| Сценарий                                            | Ответ                                                                            |
| ----------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| Двойная запись на курс                   | Ошибка 400: "Вы уже записаны на этот курс"               |
| Вторая оценка курсу                        | Ошибка 400: "Вы уже оставляли отзыв"                         |
| Доступ к заблокированному уроку | Ошибка 403: "Сначала завершите предыдущие уроки" |
| Неверный email/пароль                         | Ошибка 401: "Неверный email или пароль"                        |

Валидация:

* Email: проверка формата
* Пароль: минимум 6 символов
* Оценка: 1–5
* Телефон: произвольный формат

---

## 🧪 Тестирование API

### curl

```bash
curl -X POST http://127.0.0.1:8000/api/v1/auth/register/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "first_name": "Тест",
    "last_name": "Пользователь",
    "phone": "+79991234567",
    "password": "password123",
    "password_confirm": "password123"
  }'
```

### Браузер

* `http://127.0.0.1:8000/api/v1/courses/`
* `http://127.0.0.1:8000/api/v1/courses/python/`

### Django Admin

* `http://127.0.0.1:8000/admin/`
* Логин:** **`nikita@edu.ru` /** **`123`

---

## 📊 Проверка данных в PostgreSQL

```bash
docker exec -it lms_db psql -U postgres -d KyrsovayaBD
```

Примеры запросов:

```sql
SELECT id, email, first_name FROM users ORDER BY id DESC LIMIT 5;

SELECT u.email, e.status, e.progress_pct 
FROM enrollments e 
JOIN users u ON e.user_id = u.id 
WHERE e.course_id = 1;

SELECT AVG(rating) FROM ratings WHERE course_id = 1;
```

---

## 🎯 Рекомендации фронтенду

* Токены** ** **не хранить в localStorage** , использовать память приложения или HttpOnly cookie
* Перед истечением access token (50 минут) вызывать** **`/api/v1/auth/refresh/`
* Проверять ошибки API и редиректить на логин при 401

---

## 🚀 Запуск проекта

```bash
# Backend
cd backend
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver

# Frontend
cd frontend
python3 -m http.server 8001
```

Доступ:

* API:** **`http://127.0.0.1:8000/api/v1/`
* Frontend:** **`http://127.0.0.1:8001/`

---

## 💡 Тестовые данные

### Пользователи

| Email                     | Роль                   | Пароль |
| ------------------------- | -------------------------- | ------------ |
| `nikita@edu.ru`         | Админ                 | `123`      |
| `liza@edu.ru`           | Админ                 | `123`      |
| `python_teacher@edu.ru` | Преподаватель | `123`      |
| `student1@edu.ru`       | Студент             | `123`      |

### Курсы

| Название                        | Slug         | ID |
| --------------------------------------- | ------------ | -- |
| Python                                  | python       | 1  |
| Web-разработка                | web          | 2  |
| Data Science                            | data-science | 3  |
| Мобильная разработка | mobile       | 4  |

```markdown
# 📋 Полный технический отчёт: API для LMS на Django

...

## 📖 Документация API через Swagger

Для удобства фронтенд-разработчиков и тестирования API используется **Swagger UI** через `drf-yasg`.

### Установка

```bash
pip install drf-yasg
```

### Настройка в** **`urls.py`

```python
from django.urls import path, re_path
from rest_framework import permissions
from drf_yasg.views import get_schema_view
from drf_yasg import openapi

schema_view = get_schema_view(
   openapi.Info(
      title="LMS API",
      default_version='v1',
      description="Документация API для LMS",
      contact=openapi.Contact(email="support@example.com"),
   ),
   public=True,
   permission_classes=[permissions.AllowAny],
)

urlpatterns = [
    # ... твои API эндпоинты
    re_path(r'^swagger(?P<format>\.json|\.yaml)$', schema_view.without_ui(cache_timeout=0), name='schema-json'),
    path('swagger/', schema_view.with_ui('swagger', cache_timeout=0), name='schema-swagger-ui'),
    path('redoc/', schema_view.with_ui('redoc', cache_timeout=0), name='schema-redoc'),
]
```

### Доступ к UI

* Swagger:** **[http://127.0.0.1:8000/swagger/](http://127.0.0.1:8000/swagger/)
* ReDoc:** **[http://127.0.0.1:8000/redoc/](http://127.0.0.1:8000/redoc/)

### Использование JWT в Swagger

1. Нажмите кнопку** ****Authorize** в верхнем правом углу Swagger UI.
2. Введите токен в формате:
   ```
   Bearer <ACCESS_TOKEN>
   ```
3. Все защищённые эндпоинты будут доступны с этим токеном.
4. Для обновления токена используйте** **`/api/v1/auth/refresh/`.

### Пример запроса через Swagger

* **Endpoint** :** **`/profile/`
* **Method** : GET
* **Response** :

```json
{
  "id": 9,
  "email": "user@example.com",
  "first_name": "Иван",
  "last_name": "Иванов",
  "phone": "+79991234567",
  "avatar_url": null,
  "is_active": true
}
```

### Примечания

* Swagger автоматически показывает все эндпоинты, методы, параметры и схемы сериализаторов.
* JWT-токены можно тестировать прямо из UI, без написания curl или fetch-запросов.
* Если шаблон не найден (`swagger-ui.html`), убедитесь, что:
  * `drf-yasg` установлен
  * `'APP_DIRS': True` в** **`TEMPLATES` Django
  * `'drf_yasg'` есть в** **`INSTALLED_APPS` (необязательно, но помогает иногда)
